{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "NX_NO_CLOUD=true NX_DAEMON=false npx nx build playground --skip-nx-cache && rm -rf _site && cp -r dist/apps/playground/browser _site",
  "outputDirectory": "_site",
  "framework": null,
  "rewrites": [
    { "source": "/:path*", "destination": "/index.html" }
  ]
}
